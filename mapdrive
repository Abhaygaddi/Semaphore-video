---
- hosts: windows
  become: true  # or sudo if needed
  tasks:
    - name: Apply patch from shared location
      ansible.posix.patch:
        path: \\192.168.29.132\patches\KB5058921.msu  # Replace with the actual path
        remote_src: false
      # Optional: strip leading slashes
      # strip: 1 
