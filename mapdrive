---
- name: Copy Map.exe to remote hosts
  hosts: all  # Specify the target hosts or group
  gather_facts: no # set to no to improve speed

  tasks:
    - name: Copy Map.exe
      win_copy:
        src: '\\192.168.29.132\Patches\Map.exe'  # Use double quotes and escape backslashes
        dest: 'C:\temp\Map.exe'
        # optional:
        #  remote_user:  # Specify a different user if needed
        #  remote_pass:  # Specify password.  Use a secure method instead.
        #  become: yes       #  If you need to run with elevated privileges
        #  become_method: runas #  If you need to use runas
      #  register: copy_result #register the output

    # - name: Print the result (optional, for debugging)
    #   debug:
    #     var: copy_result
