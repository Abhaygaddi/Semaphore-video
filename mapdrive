    ---
    - hosts: windows
      become: yes  # You might need to use the appropriate 'become' or 'become_method' based on your system
      gather_facts: yes  # You might not need to gather facts for this
      tasks:
        - name: Install hotfix
          community.windows.win_hotfix:
            source: \\192.168.29.132\patches\KB5058921.msu
          # Optional parameters:
          #   force: yes  # Force the installation even if the system detects it's already installed
          #   validate: no # Disable KB and Identifier validation
          #   ignore_errors: yes # Ignore errors during installation
