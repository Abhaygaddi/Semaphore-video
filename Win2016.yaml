---
- name: Check and Install Ready Updates on Windows Server 2016 (Without Task Scheduler)
  hosts: all
  gather_facts: no
  tasks:
    - name: Ensure WinRM service is running
      win_service:
        name: WinRM
        state: started
        start_mode: auto

    - name: Start Windows Update service
      win_service:
        name: wuauserv
        state: started
        start_mode: auto

    - name: Create Log Directory
      win_file:
        path: C:\temp
        state: directory

    - name: Check for Updates Ready to Install
      win_updates:
        category_names: []  # Empty list checks for all updates
        state: searched
        use_task_scheduler: no  # Avoid using Task Scheduler
      register: update_check

    - name: Install Ready Updates and Reboot if Required
      win_updates:
        category_names: []  # Empty list installs all updates
        reboot: yes
        use_task_scheduler: no  # Avoid using Task Scheduler
      when: update_check.found_update_count > 0
      register: update_result
      ignore_errors: yes

    - name: Log Errors if Update Fails
      win_shell: |
        "$((Get-Date).ToString()) - Windows Update Failed: {{ update_result.msg }}" | Out-File -FilePath C:\temp\update_errors.log -Append
      when: update_result.failed_update_count > 0
